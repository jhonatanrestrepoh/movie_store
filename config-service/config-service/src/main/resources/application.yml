server:
  port: 9000
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///D:/Profundizar/taller_2_configs/3.SpringCloudConfig
  security:
    user:
      name: ${CONFIG_NAME}
      password: ${CONFIG_PASSWORD}

