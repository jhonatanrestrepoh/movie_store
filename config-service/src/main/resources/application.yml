server:
  port: 9000
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///D:/Profundizar/taller_2_configs/SpringCloudConfig
  security:
    user:
      name: ${CONFIG_NAME}
      password: ${CONFIG_PASSWORD}

